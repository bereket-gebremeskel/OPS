import { BekDirective } from './bek.directive';

describe('BekDirective', () => {
  it('should create an instance', () => {
    const directive = new BekDirective();
    expect(directive).toBeTruthy();
  });
});
